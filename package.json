{
  "name": "api",
  "version": "1.0.0",
  "engines": {
    "node": ">=23.1.0"
  },
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types --env-file=.env src/index.ts",
    "start:dev": "node --watch --experimental-strip-types --env-file=.env src/index.ts",
    "test": "node --test --experimental-strip-types",
    "test:dev": "node --test --watch --experimental-strip-types"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.7.9"
  },
  "dependencies": {
    "mongodb": "6.10"
  }
}